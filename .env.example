# Copiar a .env (no se commitea). MS1 — Pasajeros / Tickets.

PORT=8001
LOG_LEVEL=info
ENV=local

# MySQL 8
DB_HOST=localhost
DB_PORT=3306
DB_NAME=pasajeros
DB_USER=app
DB_PASSWORD=changeme

# MS1 llama a MS2 para validar vuelos (GET /api/vuelos/{id}/exists)
MS2_BASE_URL=http://localhost:8002
HTTP_TIMEOUT_SECONDS=3
HTTP_RETRIES=2
